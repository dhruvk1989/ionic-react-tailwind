import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic-experiment',
  appName: 'ionic-react-tailwind',
  webDir: 'dist'
};

ios: { scheme: "App" }

export default config;
